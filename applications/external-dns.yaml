apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-dns
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: external-dns
  source:
    repoURL: https://kubernetes-sigs.github.io/external-dns/
    chart: external-dns
    targetRevision: 1.21.1
    helm:
      releaseName: external-dns
      valuesObject:
        sources:
          - ingress
          - service

        provider: 
         name: rfc2136

        domainFilters:
          - linds.com.au

        extraArgs:
          # Ignore Kyverno-generated mTLS mirror Ingresses (kyverno/) so the
          # primary DNS records never get pointed at the mTLS VIP. Sources
          # without the label still match (!key = label absent).
          - --label-filter=!linds.com.au/mtls-mirror-of
          - --rfc2136-host=10.0.50.200
          - --rfc2136-port=53
          - --rfc2136-zone=linds.com.au
          - --rfc2136-insecure
          - --rfc2136-tsig-axfr
          - --fqdn-template={{ `{{ .Name }}.linds.com.au` }}
          - --service-type-filter=LoadBalancer
        registry: txt
        txtOwnerId: k8s-home

        policy: upsert-only
        logLevel: info
        interval: 1m
        serviceMonitor:
          enabled: true
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
