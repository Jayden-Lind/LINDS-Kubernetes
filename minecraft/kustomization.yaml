apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: minecraft

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"

namespace: minecraft

